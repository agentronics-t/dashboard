# intel-api — Cloud Run service
PORT=8080
LOG_LEVEL=info

# Neon Postgres (serving mirror) — from Secret Manager `neon-database-url` in prod
DATABASE_URL=

# Clerk JWT verification (STEP 4)
CLERK_ISSUER=
CLERK_JWKS_URL=

# GCP (STEP 4)
GCP_PROJECT=
GCP_REGION=asia-south1
TASKS_QUEUE=import-jobs
WORKER_URL=
TASKS_OIDC_SERVICE_ACCOUNT=
