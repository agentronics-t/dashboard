# intel-worker — Cloud Run service (Cloud Tasks HTTP target)
PORT=8080
LOG_LEVEL=info

# Neon Postgres — from Secret Manager `neon-database-url` in prod
DATABASE_URL=

# GCP (STEP 5)
GCP_PROJECT=
GCP_REGION=asia-south1
GCS_BUCKET=
ML_JOB_NAME=intel-ml
